# Workflow to run tests

name: CI

on:
  push:
    branches: [ processor_options_2.2.0-2.0.2_test ]

jobs:
  build-and-test:
    strategy:
      fail-fast: false
      matrix:
        os: [ ubuntu-latest, windows-latest ]
        is_pull_request:
          - ${{ github.event_name == 'pull_request' }}
        exclude:
          # Don't run slow targets on pull request.
          - is_pull_request: true
            os: macos-latest

    # The type of runner that the job will run on
    runs-on: ${{ matrix.os }}

    steps:
    - name: configure Pagefile
      if: matrix.os == 'windows-latest'
      uses: al-cheb/configure-pagefile-action@v1.2
      with:
        minimum-size: 8
        maximum-size: 16
        disk-root: "D:"
    - name: Setup Java 17
      uses: actions/setup-java@v1.4.3
      with:
        java-version: '17'
        java-package: jdk
        architecture: x64

    # Checkout
    - uses: actions/checkout@v2

    # Build cache
    - name: Cache Gradle Cache
      uses: actions/cache@v4
      with:
        path: ~/.gradle/caches
        key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle.kts') }}-${{ hashFiles('**/gradle.properties') }}
        # An ordered list of keys to use for restoring the cache if no cache hit occurred for key
        restore-keys: |
          ${{ runner.os }}-gradle-
    - name: Cache gradle wrapper
      uses: actions/cache@v4
      with:
        path: ~/.gradle/wrapper
        key: ${{ runner.os }}-gradle-wrapper-${{ hashFiles('gradle/wrapper/gradle-wrapper.properties') }}

    # Check API compatibility
    - name: API compatibility check
      if: matrix.os == 'ubuntu-latest'
      run: ./gradlew :api:checkApi


    - name: Integration tests
      shell: bash
      run: |
        ./gradlew publishToMavenLocal
        curl -s -L -O https://github.com/gradle/develocity-build-validation-scripts/releases/download/v2.8/develocity-gradle-build-validation-2.8.zip && unzip -q -o develocity-gradle-build-validation-2.8.zip
        cd develocity-gradle-build-validation
        ./03-validate-local-build-caching-different-locations.sh -t assembleDebug -r https://github.com/cdsap/KspNowInAndroid.git
      env:
          DEVELOCITY_ACCESS_KEY: ${{ secrets.DV_KEY }}
